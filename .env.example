# Environment Variables for Baileys Server Pro

# Server Configuration
PORT=3000
NODE_ENV=production

# Bind Address (Docker port mapping)
# For local development: BIND_ADDRESS=0.0.0.0 (accessible from host)
# For VPS/Production: BIND_ADDRESS=127.0.0.1 (localhost only - secure)
BIND_ADDRESS=127.0.0.1

# API Base URL (optional, defaults to request host)
# For VPS deployment, set this to your domain
# Example: API_BASE_URL=http://zulfadli.com:3000
API_BASE_URL=

# Optional: API Key for additional security (uncomment and set if needed)
# API_KEY=your_super_secret_api_key_here

# Optional: Webhook configuration (set per session when starting)
# WEBHOOK_URL=https://your-webhook-endpoint.com/whatsapp